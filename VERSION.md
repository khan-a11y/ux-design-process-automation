# VERSION — UX×AX 자동화 도구 모음 (UX 디자인프로세스 자동화)

> 전역 버전 규칙(~/.claude/CLAUDE.md): B(큰 변화)·P(소소수정) 카운터. 표기 `v0.`+B+(P).
> ※ 이 프로젝트는 이전 세션들에서 이미 완성·검증된 상태로, 2026-06-17에 `_desktop_회사업무관련/회사 업무관련 AX Project/260615_UX디자인프로세스/`에서 이 폴더(`05_`)로 **귀속(복사)**됨. 버전 추적은 여기서 시작.

## 현재 버전: **v1.6**  (2단계 — 판단 게이트 6칸 자가점검 도입 · P1 파일럿)

## 카운터
- B (큰 변화/새로 만들기) = **6**  (판단 게이트 시스템: dossier `gates` 모델 + 6칸 점검 패널)
- P (소소 수정) = **0**  (B 증가로 리셋 — 이후 2단계 후속(P0 합격선·재방문)부터 P+1)
- ※ 진행 중 라운드: 플랜 [PLAN.md](PLAN.md) 통합 로드맵 6단계(심층리뷰 반영). **1단계(1-0~1-3) 완료. 2단계 진행 중** — 판단 게이트 P1 파일럿 완료, 다음 = P0 합격선·재방문 루프 → 3단계에서 게이트 P2~P7 확산.
  - ※ 직전 B5(=v1.5~v1.57): 공통 아키텍처(common.js + dossier + P0 한 페이지) + 1단계 P1 재설계 7스텝.

## 상태 (인계 시점)
- UX 표준 6단계(P1~P6, +P7 운영)의 AI 자동화 과제 **29개**를 **7개 스튜디오 앱**으로 묶어 실제 자동화.
  - P1 리서치 / P2 정의 / P3 아이디에이션 / P4 콘텐츠·카피 / P5 디자인·핸드오프 / P6 품질검증 / P7 피드백·운영. (퍼소나는 별도 `88_네이버_퍼소나`)
- React 18 + Tailwind + CDN 단일 HTML, 무빌드. 무료 4엔진(브라우저AI·로컬 Ollama·복붙·API). 공유 보관함으로 산출물 연결.
- 브라우저 내 실제 처리(전사·임베딩 군집·A/B 통계·색대비·픽셀 diff), LLM은 한 단계만.
- **스모크 테스트 28/28 PASS**(index + 7앱 전 탭 렌더). `bash smoke-test.sh`. (2026-06-17 재실행 확인)

## 변경 로그
| 버전 | 날짜 | 내용 |
|------|------|------|
| v1.6 | 2026-06-24 | **[2단계 시작] 판단 게이트 6칸 자가점검 도입 (P1 파일럿).** 심층리뷰 반영 — 초급자가 "이 단계를 시작·마무리해도 되나"를 스스로 판단하는 법을 배우게. ① **common.js 게이트 모델** — dossier에 `gates[phase]` 추가, API `getGate/setGate/deriveGate` + `GATE_FIELDS`(6칸 스펙). **자동 판정 3칸**(`deriveGate`): **입력 충족**(P1=P0 브리프 name·oneline·users·success 채움 여부), **사람 검수**(해당 단계 슬롯 중 `human_confirmed` 비율), **비용·환경**(무료·브라우저). **사람 입력 3칸**: 목표·합격선(정량)·멈춤 조건(게이트 상태에 저장). ② **GatePanel** — P1 `<main>` 상단 접이식 카드, 상태칩 `점검 n/6`, auto칸 ✓충족/⚠확인+이유, text칸 textarea(저장), "이 단계 마무리 점검 통과로 표시" 토글(`confirmed`). 강제 아님(판단 보조 체크리스트). 검증: smoke **28/28** + Playwright(6칸 렌더·auto ✓2/⚠1·text→gates.P1 저장·confirmed 토글·콘솔0) + 스크린샷. **다음:** P0 합격선·재방문 루프 → 3단계에서 게이트 P2~P7 확산. |
| v1.57 | 2026-06-24 | **[1단계 1-3] 보관함 = 단계별 템플릿 뷰 + 하단 단계 네비 · 1단계 완료.** ① **보관함 재설계** — `WorkspaceModal`이 MD 덤프 대신 **dossier 슬롯을 단계별 카드**로 표시(`UXAX.SLOT_REGISTRY` 순서: P0.brief→P1.interview/survey/voc/desk/guide). 카드마다 **단계 칩·작업명·“어디서 만듦 → 다음 활용(nextLabel)”·근거 메타(모델·건수·시각)·검수 상태 뱃지**(✓검수완료/검수전/미작성) + 미리보기(md)·편집·복사·**검수 토글**(`confirmSlot`). 빈 슬롯은 점선 플레이스홀더로 “여기서 만들면 자동으로 담겨요”. 탭 토글로 **‘단계별 산출물 n/총’ ↔ ‘기타 메모’**(기존 평문 `uxax_workspace_v1`은 메모로 보존). FAB 뱃지 = 채워진 슬롯+메모 합산, 프로젝트 전체 JSON 내보내기. ② **하단 단계 네비(`StageNav`)** 신규 — (a) 같은 단계 안 **이전/다음 탭 이동**(5탭 중 n/5), (b) **이전 단계 돌아가기**(↩ 홈·P0), (c) **다음 단계로 진행**(→ P2 정의, 주버튼). 플로팅 FAB와 안 겹치게 하단 여백 확보. 검증: smoke **28/28** + Playwright(슬롯뷰·뱃지 4종·검수토글·메모탭·탭전환 #interview→#survey·다음단계 P2/이전 홈 링크·콘솔0) + 스크린샷 육안(슬롯 카드·StageNav). |
| v1.56 | 2026-06-21 | **[1단계 1-2] VOC·데스크·가이드 탭 재설계.** 설문 탭과 동일 패턴을 세 탭에 확산. ① **슬롯 저장 + 검수** — 각 결과를 dossier `P1.voc`/`P1.desk`/`P1.guide`에 자동 저장(근거 메타), `☐ 내가 검수함` 토글→`confirmSlot`(human_confirmed). ② **데스크** — ▶예제·자료 **누적 추가**(기존 덮어쓰기 제거), 출처 textarea **자동 높이**, top-K 라벨 ‘근거로 쓸 구절 수’+추천(8~12), **S1·S2 출처번호 설명**, 한국어 생성 **모델 추천 노트**(`UXAX.pickModel`→EXAONE 등). ③ **가이드** — ▶예제 **매번 다른 풀**(3종 랜덤), 유도질문·편향 점검은 생성 결과에 포함(AI 검수). ④ **VOC** — 슬롯·검수 + 결과 MD 헬퍼 정리. 검증: smoke 28/28 + Playwright(슬롯 3종 type·검수버튼 렌더·데스크 누적 1→2·콘솔0) + 스크린샷 육안. |
| v1.55 | 2026-06-19 | **[1단계 1-1] 설문 탭 재설계(자기설명형).** ① **용어 순화** — 탭명 ‘설문 코딩’→**‘설문 자유응답 분류’**, ‘코드북’→‘분류 기준표(코드북)’, ‘배치’→‘한 번에 처리할 묶음(배치)’, ‘코딩 실행’→‘자동 분류 실행’ 등 비전문가용으로. ② **전건 미리보기** — 예제/CSV 로드 후 응답을 **표로 전건 표시**(예전엔 1건만), ID열 라벨 ‘응답 고유번호(선택) — 없으면 자동 번호’. ③ **가져오기·CSV 안내** — 보관함에서 가져오기(WsPull) + 엑셀/시트 복붙 안내 + **예시 CSV 다운로드**. ④ **설정 인라인 설명·추천값** — 분류 관점·묶음 크기에 추천치(작은모델 10~15/API 25~40). ⑤ **슬롯 저장 + 검수** — 결과를 dossier `P1.survey`에 자동 저장(근거 메타), **‘☐ 내가 검수함’ 토글→`confirmSlot`**(human_confirmed). 검증: smoke 28/28 + Playwright(미리보기 12건·용어·슬롯 type=survey·검수 토글). |
| v1.54 | 2026-06-19 | **[1단계 1-0] 공통 기반 보강(심층리뷰 반영).** ① **슬롯 근거 추적 메타** — `setSlot`이 모든 슬롯에 `source_id/source_type/confidence/human_confirmed/used_in_phase` 기본값 부여, `confirmSlot(id)`로 사람 검수 표시(AI 제안→사람 결정). ② **프롬프트 중앙화** — P1의 LLM 프롬프트 7개(요약·코드북·코딩·VOC라벨·우선순위·데스크·가이드) **본문**을 `common.js`의 `UXAX.PROMPTS`로 이전, P1은 데이터 정형만 하고 위임(7파일 드리프트 방지·출력 바이트 동일). ③ 외부 리뷰용 GitHub 공개 레포 생성(`khan-a11y/ux-design-process-automation`, config.js 등 시크릿 제외). 검증: smoke 28/28 + Playwright(프롬프트 출력 동일·메타 기본값·confirmSlot·콘솔0). |
| v1.53 | 2026-06-18 | **[P1 재설계] Step 3 — 인터뷰 탭.** ① 텍스트화 모델 **기본값 large-v3-turbo(최고 정확도)**. ② **AudioCard**: 기본 플레이어 `controlsList="nodownload noplaybackrate"`로 ⋮ 메뉴의 재생속도·다운로드 **중복 제거**, 다운로드는 **‘⬇ 다운로드’ 라벨 버튼**, 삭제는 **🗑 휴지통**. ③ 전사 진행 = **추정 % 진행 바**(오디오 길이 기반)+경과·안내(원형 스피너 대체, 정확 ETA는 라이브러리 미제공이라 추정). ④ **‘WebGPU 가속 사용’ 문구 제거**(미지원 시 경고만). ⑤ **드래그앤드롭 active 상태**(들어오면 색·아이콘·문구 변화). ⑥ **② 결과 = 클로바식 레이아웃** — 화자별 색 블록(연속 화자 묶음)·타임스탬프 재생·읽기 좋은 문장 textarea, 기능(원문복사·AI요약)은 **상단 액션바**로 통합. ⑦ 결과를 dossier `P1.interview` 슬롯에 저장. Playwright 통과(turbo기본·controlsList·라벨·🗑·화자블록·콘솔0). |
| v1.52 | 2026-06-18 | **[P1 재설계] Step 2 — 공통 레이어.** ① 각 탭 상단 `Guide`를 **항상 보이는 ‘🎯 이 도구로: …’ 1줄 + [📖 사용방법] 버튼(상세는 모달)**로 전환(긴 가이드 블록 제거). ② **플로팅 학습 멘토**(`MentorFab`, 우하단 따라다님) 신규 — 탭별 **추천 질문 3~5개 칩**(FAQ 즉답·무료) + 자유 질문(AI) + **Q&A를 dossier `mentorLog`에 저장 → ‘내 질문’ 탭에서 복습**(유튜브 제미나이식). 기존 인라인 `?`(HelpCue)는 유지. Playwright 통과(자기설명·모달·멘토 즉답·저장·모아보기·탭별 추천변경·콘솔0). |
| v1.51 | 2026-06-18 | **P0 폼 레이아웃 UX 개선(사용자 피드백).** ① **필드별 의도된 크기** — 짧은 칸(이름 1줄·대상/성공/제약 2줄·½폭)은 작게, 길어질 칸(배경·문제, 현재 문제점/가설/변경범위 5줄·전체폭)은 크게. 자동 높이는 필드별 최소 줄수(`data-min`) 보장하도록 수정(짧은 칸이 쓸데없이 커지던 것 해결). ② **안내를 박스 안 placeholder로** — 비었을 때 무엇을 쓸지 박스 안에 회색으로 보임(별도 회색 줄 제거). ③ 2열 그리드(이름+대상, 성공+제약 / 한줄·배경·현재문제점은 전체폭). Playwright 검증: 빈 상태 높이 41/64/133px 차등·placeholder 노출·예시 채움 값↔라벨 정확·**옛 위저드 데이터 마이그레이션 시에도 라벨↔값 정확**(사용자가 본 어긋남은 코드버그 아닌 이전 테스트 stale 데이터로 확인). |
| v1.5 | 2026-06-18 | **[P1 기준 모델 재설계 라운드 시작] Step 0~1.** 사용자 P1 전수 피드백 → "기능 패널 묶음 → 초보가 따라가며 배우는 자기설명형 흐름 도구"로 방향 전환(플랜: `~/.claude/plans/transient-skipping-emerson.md`). **Step 0(기반):** ① `common.js` 신규 — 7파일 복제 구조의 drift 방지 위해 평문 공통로직을 `window.UXAX` 네임스페이스로 추출(babel JSX는 파일별 유지). ② **프로젝트 dossier 데이터모델**(`uxax_project_v1`) — 산출물이 슬롯(P1.interview/survey/voc/desk/guide…)에 `{data,md,meta}`로 들어감 + 멘토 학습로그 + 작업별 모델 라우팅(전사=large-v3-turbo, 한국어 생성=EXAONE 우선). 기존 `uxax_project_brief_v1`·`uxax_workspace_v1` 공존/마이그레이션. ③ index·P1에 common.js 로드(babel 충돌 0). **Step 1(P0 한 페이지 폼):** 7스텝 위저드 → **한 페이지 양식**(전 필드 한눈, 유형 항상 노출 → '이전 못 감' 문제 해소), **▶예시 한 번에 채우기**, 자동 높이 textarea, 자동저장→dossier.brief. Playwright e2e 통과(필드 6→7·예시일괄·dossier저장·새로고침복원·P1 자동프리필 호환·index/P1 콘솔에러0). **남음:** Step 2~6 = P1 자기설명·플로팅 멘토·인터뷰/설문/VOC/데스크/가이드 재설계·보관함 템플릿 뷰. |
| v1.42 | 2026-06-18 | **퍼소나 스튜디오 링크 경로 정정** — 퍼소나 앱이 `88_네이버_퍼소나` → `260618_퍼소나_스튜디오`로 리네임/이동되며 깨진 fallback 경로를 `../260618_퍼소나_스튜디오/v0.2/persona-studio.html`로 갱신. `index.html`·`P2_정의스튜디오.html` 2곳 + README·HANDOFF_상세 문서 정정. **야간 정적 검수(저녁노예)가 발견** — v1.01 때 예측한 '폴더 다시 옮기면 깨짐' 케이스가 실제 발생. (영구 안전책: `config.js`의 `personaStudioUrl` 절대경로 권장.) |
| v1.41 | 2026-06-18 | **P0 고도화 — 진짜 연동.** ① **P1 ‘리서치 맥락’ 자동 프리필**: P1 인터뷰 탭 진입 시 P0 브리프(한줄정의·대상·배경·가설/현재문제/변경범위·성공기준)를 맥락 칸에 **자동으로 채움** + ‘📋 프로젝트 정의 ‘…’ 연결됨’ 칩 + ‘맥락 다시 가져오기’ 버튼(이미 입력돼 있으면 덮지 않음). ② P0 요약 **↧ MD 다운로드** 추가. ③ 핵심칸(이름·한줄정의) 비면 요약에 **부드러운 안내**(차단 아님). Playwright e2e 통과(MD다운로드·P1 자동프리필·연결칩·양쪽 콘솔에러0). |
| v1.4 | 2026-06-18 | **index에 P0 ‘프로젝트 정의’ 단계 신규 — 리서치 시작 전 브리프.** 사용자 지적(“리서치 전에 프로젝트를 먼저 정의해야 하는 거 아니냐”) 반영. ① **유형 선택**(신규/개선/업데이트)으로 시작 → 유형별 맞춤 질문(신규=핵심가설·개선=현재문제점·업데이트=변경범위) ② **좌우 화살표 위저드**(← 이전 / 다음 →, 진행 점·n/N) + 칸마다 **▶예시로 채우기**(빈화면 공포 제거) ③ **자동저장**(`uxax_project_brief_v1`, 입력 즉시·새로고침 복원) ④ **보관함 연동**: 완료 시 ‘📤 보관함에 저장’ → 공유 보관함(`uxax_workspace_v1`)에 MD로 들어가 **P1~P7 어디서든 📥로 맥락 사용** ⑤ ‘🔍 리서치 시작 (P1) →’ 동선 ⑥ index에 **에러 로거**(window.onerror 화면 패널) 추가(실행형 산출물 공통). 바닐라 JS(허브 즉시 렌더 유지). Playwright e2e 통과(타입3·7스텝·자동저장·복원·보관함 연동·콘솔에러0). **주의:** v0(walking skeleton) — P1 ‘리서치 맥락’은 아직 보관함에서 *수동* 가져오기(자동 프리필 미연결), 필수칸 검증·키보드 화살표(텍스트 편집 충돌로 보류, Ctrl+Enter=다음만) 미적용. |
| v1.3 | 2026-06-18 | **사용자 피드백 반영 — 전사 품질 + P1 인터뷰 대개편.** ① 전사 깨짐(‘사사사’·‘----’) 수정: decoder q4→**q8 우선** + `no_repeat_ngram_size`·`temperature:0` 반복방지 + 모델 base→**small 기본·large-v3-turbo 추가**. ② P1 인터뷰 재구성: **멀티 오디오 업로드** · 파일별 플레이어(재생속도·다운로드·삭제·볼륨·시간) · **모델 선택 전사 비교**(결과에 모델명·소요시간) · 2단 레이아웃 · ‘전사’→‘텍스트화’ 용어 · 리서치맥락 좌우 위계 · 입력가이드 중심. ③ 공통(P1만): 텍스트 `zoom:1.2` · Guide 기본 접힘. Playwright 9/9. **주의:** zoom·가이드접힘·텍스트화 용어는 **P1만 적용(P2~P7 미확산)**, large-v3-turbo 로딩·한국어 품질은 **실오디오 미검증**(브라우저 무료 한국어는 구조적 한계 — 정확도 필요시 clova·Purfview 로컬 전사 후 ‘붙여넣기’ 권장). |
| v1.2 | 2026-06-17 | **학습 레이어 7개 앱 전체 확산(Phase 3) + 단계 간 연동(Phase 4).** P2~P7 6개에 프로세스 내비·인라인 ?버튼·▶예제로 해보기·‘저장하고 다음 단계로’ 적용. 같은 앱 탭 이동용 `hashchange` 리스너 추가. 끊겼던 연동(P5→P6·P6↔P7) + **P7→P3 개선 순환** 동선 연결. Playwright 기능검증 앱별 통과(예제·?버튼·hashchange·내비), 보관함 7앱 공유 end-to-end 확인, smoke 28/28. 다음: 실무 MVP 고도화(Phase 5). |
| v1.12 | 2026-06-17 | **P1 학습 레이어 완성(Phase 2).** 나머지 4탭(인터뷰·설문·데스크·가이드)에 ▶예제로 해보기·인라인 ?버튼(초급 FAQ+AI 멘토)·‘저장하고 다음 단계로’ 적용. 예제를 중고거래 ‘첫 거래 신뢰’ 시나리오로 5탭 통일. P1 5탭 콘솔에러0 렌더 확인. 다음: P2~P7 확산. |
| v1.11 | 2026-06-17 | **[긴급] 7개 스튜디오 전체 렌더 깨짐 수정.** `@babel/standalone`을 버전 미지정(latest)으로 불러와, 최신 Babel이 JSX를 automatic runtime으로 컴파일하며 `import .. from "react/jsx-runtime"`를 일반 `<script>`에 삽입 → `file://`(비모듈)에서 `Uncaught SyntaxError: Cannot use import statement outside a module`로 스피너만 무한 표시되던 문제. 7개 파일 모두 `Babel.registerPreset`으로 **classic runtime(React.createElement) 강제**해 해결. CDN latest 변동에 따른 시한폭탄이었음(내 직전 편집 무관, 전역 문제). smoke 28/28 PASS + 7개 실제 마운트 확인. |
| v1.1 | 2026-06-17 | **초급자 학습 레이어 도입(walking skeleton).** P1에 ①전체 UX 프로세스 내비(P1→P7 현재위치 강조·클릭 이동) ②인라인 **?버튼**(초급 FAQ + AI 멘토 추가질문) 공통 컴포넌트 추가. VOC 탭에 ③원클릭 **‘예제로 해보기’**(중고거래 샘플 12건) ④**‘저장하고 다음 단계로’** 동선 적용. smoke 28/28 유지. **다음:** P1 나머지 4탭 → 7개 앱 확산 + 연동 끊김(P5→P6·P6↔P7) 수리. |
| v1.01 | 2026-06-17 | **퍼소나 스튜디오 링크 상대경로 수정** — `05_` 폴더 귀속으로 깊이가 바뀌어 `../../../88_네이버_퍼소나/`(원본 3단계 깊이 기준)가 깨져 있던 것을 `../88_네이버_퍼소나/`로 정정. `index.html`·`P2_정의스튜디오.html` 2곳. 영구 상세 핸드오프(`HANDOFF_상세.md`) 신규 작성. |
| v1.0 | 2026-06-17 | `05_` 폴더로 귀속(복사). 이전 세션들에서 7개 스튜디오·29개 도구 완성, 스모크 28/28 통과 상태. 디자이너용 입문 덱(04) 500 섹션에 프로덕트로 추가됨. |

## 주의
- 원본 위치 `_desktop_회사업무관련/…/260615_UX디자인프로세스/`는 **삭제 완료**(2026-06-17, 사용자 승인). 이 폴더(`05_`)가 **정본**.
- ~~drift 없음~~ → **정정(v1.01):** 폴더 귀속 시 **퍼소나 링크 상대경로가 깨져 있었음**(파일 경로 의존 코드의 전형적 이동 부작용). v1.01에서 수정 완료. `config.js`의 `personaStudioUrl`로 덮어쓰면 경로 무관하게 동작.
- `file://` 더블클릭으로 동작하나, 일부 무거운 기능(전사 등)은 `serve.bat`(로컬 서버)로 여는 게 안정적. 헤드리스 단순 렌더 시 스튜디오 로딩이 길 수 있음(허브 index.html은 즉시 렌더).
- 비밀키는 `config.js`(gitignore 대상)로 분리, `config.example.js`만 공유.
